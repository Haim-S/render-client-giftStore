
import styled from "@emotion/styled";
import { Link } from "react-router-dom";

export const SidebarStyle = styled.div((props) => ({

    width: "300px",
    height: "100vh",
    position: "fixed",
    backgroundColor: "var(--sidebar-bgc-color)",
    display: "none",

}));

export const inputStyle = styled.input((props) =>({

}));



